{
  "name": "@lbrtw/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @lbrtw/shared build && nuxi dev --no-fork --host 0.0.0.0 --port 3000",
    "build": "pnpm --filter @lbrtw/shared build && nuxi build",
    "start": "node .output/server/index.mjs",
    "preview": "nuxi preview --host 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "@lbrtw/shared": "workspace:*",
    "nuxt": "3.15.1"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  }
}
